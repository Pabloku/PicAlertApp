# Task: [Setup - Database] AppDatabase

**Phase**: Phase 1 - Setup / Foundation

## Description
Create the Room database in `core/database/AppDatabase.kt`, temporarily empty or with initial configuration, and provide it through Hilt.

## Acceptance Criteria
- [ ] Abstract class extending `RoomDatabase`.
- [ ] `Room.databaseBuilder` is provided in `DatabaseModule.kt`.
