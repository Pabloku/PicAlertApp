# Task: [Story 1 - Domain] Use Cases

**Phase**: Phase 2 - Story 1 (MVP)

## Description
Create `SaveTutorEmailUseCase.kt` and `GetTutorEmailUseCase.kt` in `feature/onboarding/domain/`.

## Acceptance Criteria
- [ ] Basic email format validation logic exists in the save use case.
- [ ] They call the data layer (`DataStore`) correctly.
